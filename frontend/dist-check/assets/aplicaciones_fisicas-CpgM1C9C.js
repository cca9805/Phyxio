const e=`# Aplicaciones fisicas

## 1. Planos inclinados

Aparece cuando una magnitud dirigida debe calcularse, compararse o interpretarse en planos inclinados. Se calcula una componente, resultante, proyeccion, orientacion o vector asociado segun la pregunta. El resultado significa un efecto fisico con modulo, direccion, sentido y unidad. El error peligroso es reducirlo a un numero sin conservar la geometria vectorial.

## 2. Rotacion

Aparece cuando una magnitud dirigida debe calcularse, compararse o interpretarse en rotacion. Se calcula una componente, resultante, proyeccion, orientacion o vector asociado segun la pregunta. El resultado significa un efecto fisico con modulo, direccion, sentido y unidad. El error peligroso es reducirlo a un numero sin conservar la geometria vectorial.

## 3. Sistemas moviles

Aparece cuando una magnitud dirigida debe calcularse, compararse o interpretarse en sistemas moviles. Se calcula una componente, resultante, proyeccion, orientacion o vector asociado segun la pregunta. El resultado significa un efecto fisico con modulo, direccion, sentido y unidad. El error peligroso es reducirlo a un numero sin conservar la geometria vectorial.

## 4. Optica

Aparece cuando una magnitud dirigida debe calcularse, compararse o interpretarse en optica. Se calcula una componente, resultante, proyeccion, orientacion o vector asociado segun la pregunta. El resultado significa un efecto fisico con modulo, direccion, sentido y unidad. El error peligroso es reducirlo a un numero sin conservar la geometria vectorial.

## 5. Tensores iniciales

Aparece cuando una magnitud dirigida debe calcularse, compararse o interpretarse en tensores iniciales. Se calcula una componente, resultante, proyeccion, orientacion o vector asociado segun la pregunta. El resultado significa un efecto fisico con modulo, direccion, sentido y unidad. El error peligroso es reducirlo a un numero sin conservar la geometria vectorial.
`;export{e as default};
