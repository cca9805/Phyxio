const e=`# Transformer models

## Ideal model

The ideal transformer model describes a device with two windings perfectly coupled through a ferromagnetic core of infinite permeability. In this model, all magnetic flux generated by the primary completely links with the secondary, without leakage. Voltages follow the turns ratio [[N_1]]/[[N_2]] directly, while currents follow the inverse relation.

In the ideal model, apparent power is conserved between primary and secondary. There are no losses or internal voltage drops. Efficiency [[eta]] takes its ideal maximum value, and losses [[P_p]] are zero. This model is the conceptual basis for transformer analysis and allows quick first-approximation calculations.

## Hypotheses

The ideal model is based on the following simplifying hypotheses. Magnetic coupling between windings is perfect, without flux leakage. The core has infinite magnetic permeability, requiring negligible magnetisation current. Winding resistances are zero, so there is no resistive voltage drop or Joule effect losses. There are no iron losses due to hysteresis or eddy currents. The transformer operates at sinusoidal steady state.

## Quantitative validity domain

The ideal model is valid when the transformer operates near its nominal point with linear loads. Quantitatively, the ideal model error is less than 5% when the load current is between 25% and 100% of nominal. For modern power transformers with efficiency above 98%, the ideal model provides results with error below 2% in voltage calculations. Apparent power must be maintained within 95% to 100% of the nominal value [[S]] to maintain model accuracy.

## Model failure signals

The ideal model ceases to be reliable in the following situations. At no-load, the model predicts zero primary current, but in reality the magnetisation current flows, which can represent between 2% and 10% of nominal. Under short circuit, the model predicts infinite current, while in reality internal impedance limits the current to a finite value typically between 10 and 20 times nominal. At full load, the model does not predict the internal voltage drop typically of 2% to 5%. When applied voltage exceeds 110% of nominal, core saturation causes linear relationships to cease holding.

## Extended or alternative model

Progressive extensions exist to correct the limitations of the ideal model. The transition to an extended model is recommended when the operating conditions exceed the quantitative validity limits of the ideal model, or when the application requires precision beyond what the linear approximation can provide.

When to switch to the extended model: when experimental data differs from the ideal model by more than 5 percent; when modelling voltage drop under load is required, switch to the model with winding resistances; when precision in calculating efficiency at partial loads is needed, change to the model with separate copper and iron losses.

The model with winding resistances includes the electrical resistance of the primary and secondary as internal parameters not handled by the basic calculator of this leaf. This allows resistive voltage drops and copper losses [[P_Cu]] to be calculated with precision. Load current now produces a measurable reduction in [[V_2]] relative to the ideal value.

The model with magnetic losses separated distinguishes iron losses [[P_Fe]], considered approximately constant at fixed voltage, from copper losses [[P_Cu]], which vary with the square of current. This model allows maximum efficiency to be predicted, typically at 50-75% load.

The complete model with leakage impedance includes the leakage reactance due to flux that does not link both windings. It is necessary to calculate exact voltage regulation and short-circuit current. This model is essential for power transformer design and for short-circuit analysis in electrical networks.

## Operational comparison

The ideal model is sufficient for quick estimation calculations, preliminary transformer selection and analysis of basic operating principles. It provides results with error below 5% for high-quality transformers operating between 75% and 100% of nominal load.

The transition to the model with separate losses is necessary when efficiency must be calculated with precision above 1%, when the point of maximum efficiency must be determined, or when the transformer frequently operates at partial loads. This model is the standard for commercial specifications and manufacturer datasheets.

The complete model with leakage impedance is indispensable for short-circuit calculations, protection coordination, voltage regulation analysis and large power transformer design. The transition to this model is justified when the extended model error exceeds 2% or when behaviour under fault conditions must be predicted.
`;export{e as default};
