const e=`# Practica

## Nivel 1: reconocimiento

Identifica en tres nubes de puntos cual parece lineal, cual parece curva exponencial y cual parece ley de potencia. Escribe que eje transformarias en cada caso antes de ajustar.

Señala en una recta ajustada la pendiente, el corte y un residuo. Explica en una frase que podria significar cada uno en un experimento.

## Nivel 2: aplicacion directa

Calcula la pendiente y el corte de una recta que pasa por \`(2, 5)\` y \`(6, 13)\`. Despues asigna unidades si los ejes son \`F (N)\` y \`x (m)\`.

Para \`ln(y)\` frente a \`t\`, calcula la pendiente entre \`(0 s, 4.0)\` y \`(10 s, 3.0)\`. Decide si representa crecimiento o decaimiento y escribe la unidad.

## Nivel 3: eleccion de herramienta

Elige si conviene grafica lineal, semilog o log-log para: resorte, decaimiento radiactivo, intensidad con distancia y calibracion de sensor. Justifica cada eleccion desde el modelo.

Compara dos ajustes con la misma pendiente pero residuos distintos. Explica cual aceptarias y que patron de residuos te haria rechazar el modelo.

## Nivel 4: interpretacion fisica

Interpreta una pendiente \`250 N/m\` en un ajuste fuerza-estiramiento. Indica que significa, en que intervalo vale y que podria indicar un corte no nulo.

Interpreta una pendiente \`-0.12 1/s\` en una grafica \`ln(A)\` frente a tiempo. Explica por que el signo negativo es fisicamente esperado en amortiguamiento.

## Nivel 5: error intencionado

Un alumno transforma un decaimiento con logaritmos, obtiene corte \`2.3\` y afirma que la amplitud inicial era \`2.3 m\`. Localiza el error y corrige recordando que el corte es \`ln(A0)\`.

Otro alumno elige la transformacion que produce la recta mas bonita sin justificar modelo. Explica por que eso no prueba una ley fisica y redacta una conclusion limitada y verificable.
`;export{e as default};
