const e=`# Practice

## Level 1: recognition

Identify the axes and units of a graph \`v (m/s)\` versus \`t (s)\`. Write which quantity is on the horizontal axis, which quantity is on the vertical axis, and what any point on the curve means.

Classify three intervals as increasing, decreasing, or constant. Explain in one physical sentence what each interval means for the motion.

## Level 2: direct application

Calculate by approximate reading the position of an object at \`6 s\` on a graph \`x (m)\` versus \`t (s)\`. Keep the unit and state whether the value is exact or interpolated.

Detect the instant when a temperature graph reaches \`50 C\`. Check that you are reading the horizontal axis, not the vertical axis.

## Level 3: choosing the tool

Choose whether graph reading is enough or whether slope is needed: "what is the position at \`3 s\`?", "at what rate does position change?", "when does velocity become zero?". Justify each choice before calculating.

Compare two graphs with different scales and explain why counting squares is not enough. Write which labels you must check to make a valid comparison.

## Level 4: physical interpretation

Interpret a graph of potential energy versus height that increases linearly. Explain what the rising curve means and what unit any value read on the vertical axis would have.

Analyze an amplitude-frequency graph with a marked maximum. State which physical question the maximum answers and what you cannot conclude without an additional model.

## Level 5: intentional error

A student reads a velocity-time graph and writes: "at \`t = 4 s\`, \`v = -3 m/s\`; therefore speed is negative and the object moves less". Detect the error, explain why sign is not magnitude, and correct the interpretation.

Another student extrapolates an experimental straight line far beyond the measured data. Correct the conclusion by stating the graph domain and what condition would have to justify extrapolation.
`;export{e as default};
