const e=`# Practice

## Level 1: recognition

Identify in three data clouds which one looks linear, which one looks exponential, and which one looks like a power law. Write which axis you would transform in each case before fitting.

Point out slope, intercept, and one residual on a fitted line. Explain in one sentence what each could mean in an experiment.

## Level 2: direct application

Calculate slope and intercept of a line passing through \`(2, 5)\` and \`(6, 13)\`. Then assign units if the axes are \`F (N)\` and \`x (m)\`.

For \`ln(y)\` versus \`t\`, calculate the slope between \`(0 s, 4.0)\` and \`(10 s, 3.0)\`. Decide whether it represents growth or decay and write the unit.

## Level 3: choosing the tool

Choose whether a linear, semilog, or log-log graph is suitable for: spring, radioactive decay, intensity with distance, and sensor calibration. Justify each choice from the model.

Compare two fits with the same slope but different residuals. Explain which one you would accept and which residual pattern would make you reject the model.

## Level 4: physical interpretation

Interpret a slope \`250 N/m\` in a force-extension fit. State what it means, over which interval it is valid, and what a nonzero intercept might indicate.

Interpret a slope \`-0.12 1/s\` in a graph of \`ln(A)\` versus time. Explain why the negative sign is physically expected in damping.

## Level 5: intentional error

A learner transforms a decay with logarithms, obtains intercept \`2.3\`, and claims that the initial amplitude was \`2.3 m\`. Locate the error and correct it by recalling that the intercept is \`ln(A0)\`.

Another learner chooses the transformation that produces the prettiest line without justifying a model. Explain why this does not prove a physical law and write a limited, testable conclusion.
`;export{e as default};
