#!/usr/bin/env node
// DEPRECATED: generateSidebarFromEducationHierarchy
// The separate 'niveles' generation pipeline has been removed. Keep a no-op
// script to avoid breaking calls from tooling or CI.

console.log('generateSidebarFromEducationHierarchy: deprecated — no action taken.');
process.exit(0);
