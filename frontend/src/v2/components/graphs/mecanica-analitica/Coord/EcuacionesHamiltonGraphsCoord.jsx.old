/**
 * createCoordGraph(
 *   leaf: "ecuaciones-de-hamilton",
 *   pregunta_principal: "¿Cómo reescriben las ecuaciones de Hamilton la dinámica para seguir la evolución en espacio de fases con variables conjugadas?",
 *   formula: "qdot = p/m, pdot = -k*q",
 *   XAxis: "Coordenada canónica: [[qi]]",
 *   YAxis: "Flujo de fase: [[qd]], [[pd]]",
 *   axes: { x: "[[qi]]", y: "[[qd]] / [[pd]]" },
 *   target: "[[qd]], [[pd]]",
 *   paramsIn: ["[[m]]", "[[k]]", "[[p]]"],
 *   stateOut: ["[[qd]]", "[[pd]]"],
 *   sharedParams: { __calc: "hamilton" }
 * )
 */
const uiLang = "es";
const tt = (es, en) => (uiLang === "en" ? en : es);
import profile from "./EcuacionesHamiltonGraphsCoord.profile.js";

void profile;

export { default } from "./MecanicaAnaliticaGraphsCoord.jsx";
