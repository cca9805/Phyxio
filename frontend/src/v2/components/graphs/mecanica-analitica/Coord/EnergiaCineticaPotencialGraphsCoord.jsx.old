/**
 * createCoordGraph(
 *   leaf: "energia-cinetica-y-potencial",
 *   pregunta_principal: "¿Como se construye el lagrangiano a partir de energia cinetica y potencial para describir la dinamica con coordenadas generalizadas?",
 *   formula: "L = T - V, T = 1/2*m*v^2, V = m*g*h",
 *   XAxis: "Velocidad ordinaria: [[v]]",
 *   YAxis: "Energias: [[T]], [[V]]",
 *   axes: { x: "[[v]]", y: "[[T]] / [[V]]" },
 *   target: "[[L]]",
 *   paramsIn: ["[[m]]", "[[h]]", "[[g]]"],
 *   stateOut: ["[[T]]", "[[V]]", "[[L]]"],
 *   sharedParams: { __calc: "energia" }
 * )
 */
const uiLang = "es";
const tt = (es, en) => (uiLang === "en" ? en : es);
import profile from "./EnergiaCineticaPotencialGraphsCoord.profile.js";

void profile;

export { default } from "./MecanicaAnaliticaGraphsCoord.jsx";
