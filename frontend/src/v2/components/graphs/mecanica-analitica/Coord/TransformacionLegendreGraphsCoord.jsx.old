/**
 * createCoordGraph(
 *   leaf: "transformacion-legendre",
 *   pregunta_principal: "¿Cómo permite la transformación de Legendre pasar de una descripción lagrangiana en velocidades a una descripción hamiltoniana en momentos conjugados?",
 *   formula: "H = p*qdot - L, p = m*qdot",
 *   XAxis: "Velocidad generalizada: [[qdi]]",
 *   YAxis: "Momento conjugado y hamiltoniano: [[p]], [[H]]",
 *   axes: { x: "[[qdi]]", y: "[[p]] / [[H]]" },
 *   target: "[[H]]",
 *   paramsIn: ["[[m]]", "[[L]]"],
 *   stateOut: ["[[p]]", "[[H]]"],
 *   sharedParams: { __calc: "legendre" }
 * )
 */
const uiLang = "es";
const tt = (es, en) => (uiLang === "en" ? en : es);
import profile from "./TransformacionLegendreGraphsCoord.profile.js";

void profile;

export { default } from "./MecanicaAnaliticaGraphsCoord.jsx";
