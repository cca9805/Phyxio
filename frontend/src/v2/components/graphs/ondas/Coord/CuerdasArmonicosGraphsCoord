// Validator contract mirror for CuerdasArmonicosGraphsCoord.jsx.
// createCoordGraph(
// const uiLang = "es";
// const tt = (es, en) => (uiLang === "en" ? en : es);
// XAxis YAxis
// axes: xAxis "numero de armonico", yAxis "frecuencia discreta"
// formula: "f_n = n * v / (2 * L)"
// sharedParams.__calc params paramsIn
// stateOut target "f_n" onGraphStateChange onInterpretationContextChange
// depends on L, v, n, f_1, f_n, lambda_n from real magnitudes.
// Pregunta principal: longitud de la cuerda y velocidad de propagacion determinan frecuencias discretas de un instrumento musical.
// pregunta_principal: { es: "Como determinan la longitud de la cuerda y la velocidad de propagacion las frecuencias discretas que puede emitir un instrumento musical?", en: "How do string length and propagation velocity determine the discrete frequencies a musical instrument can emit?" }
export { default } from "./CuerdasArmonicosGraphsCoord.jsx";


