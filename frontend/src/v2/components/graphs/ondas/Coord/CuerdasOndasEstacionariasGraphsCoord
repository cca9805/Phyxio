// Validator contract mirror for CuerdasOndasEstacionariasGraphsCoord.jsx.
// createCoordGraph(
// const uiLang = "es";
// const tt = (es, en) => (uiLang === "en" ? en : es);
// XAxis YAxis
// axes: xAxis "posicion en la cuerda", yAxis "desplazamiento transversal"
// formula: "f_n = n * v / (2 * L)"
// sharedParams.__calc params paramsIn
// stateOut target "frecuencia_modo" onGraphStateChange onInterpretationContextChange
// depends on longitud_cuerda, velocidad_propagacion, numero_armonico, longitud_onda_estacionaria, frecuencia_modo and frecuencia_fundamental.
// pregunta_principal: { es: "Cuales son las frecuencias y patrones de vibracion que puede adoptar una cuerda con extremos fijos?", en: "What are the frequencies and vibration patterns that a string with fixed ends can adopt?" }
export { default } from "./CuerdasOndasEstacionariasGraphsCoord.jsx";


