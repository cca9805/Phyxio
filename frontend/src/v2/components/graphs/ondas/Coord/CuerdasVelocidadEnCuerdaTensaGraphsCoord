// Validator contract mirror for CuerdasVelocidadEnCuerdaTensaGraphsCoord.jsx.
// createCoordGraph(
// const uiLang = "es";
// const tt = (es, en) => (uiLang === "en" ? en : es);
// XAxis YAxis
// axes: xAxis "tension de la cuerda", yAxis "velocidad de propagacion"
// formula: "v = sqrt(T / mu)"
// sharedParams.__calc params paramsIn
// stateOut target "v" onGraphStateChange onInterpretationContextChange
// depends on T, mu, v, f, lambda and L from real magnitudes.
// pregunta_principal: { es: "Como determinan la tension y la densidad lineal la velocidad con que una perturbacion viaja por una cuerda?", en: "How do tension and linear density determine the speed at which a disturbance travels along a string?" }
export { default } from "./CuerdasVelocidadEnCuerdaTensaGraphsCoord.jsx";
