import React from "react";
import { createCoordGraph } from "../../graphFactories.jsx";

export const uiLang = "es";
export const tt = (es, en) => (uiLang === "en" ? en : es);
void uiLang;
void tt;

export const graphContract = {
  meta: {
    title: {
      es: "Evolución Temporal de la Aceleración",
      en: "Temporal Evolution of Acceleration"
    },
  },
  graph_identity: {
    pregunta_principal: {
      es: "¿Cómo evoluciona la aceleración en rodadura pura?",
      en: "How does acceleration evolve in pure rolling?",
    },
    magnitud_estrella: "a",
    variable_control: "theta",
  },
  axes: {
    x: "tiempo (s)",
    y: "aceleración (m/s²)"
  },
  formula: "a = g·sinθ / (1 + β)",
};

function AnalisisDinamicoDeLaRodaduraGraphsCoord(props) {
  return <div className="v2-card">Coord Graph Placeholder</div>;
}

export default createCoordGraph({
  Component: AnalisisDinamicoDeLaRodaduraGraphsCoord,
  displayName: "AnalisisDinamicoDeLaRodaduraGraphsCoord",
});
