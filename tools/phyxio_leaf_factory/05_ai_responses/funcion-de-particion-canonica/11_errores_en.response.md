# ------------------------------------------------------------
# Phyxio Leaf Factory | AI Markdown Response
# ------------------------------------------------------------
# Leaf ID: funcion-de-particion-canonica
# Target response file: errores.en.md
#
# Pega aquí la respuesta markdown devuelta por la IA.
# No pegues explicaciones, solo el contenido markdown válido.
# ------------------------------------------------------------
# PENDING_AI_RESPONSE

# Canonical Partition Function
## Conceptual errors
### Error 1: confusing Z with a probability
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

### Error 2: assuming all microstates weigh the same
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

## Model errors
### Error 3: using the canonical ensemble without a heat bath
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

### Error 4: forgetting degeneracy
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

## Mathematical errors
### Error 5: normalizing before summing all weights
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

### Error 6: treating [[beta]] as temperature
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

## Interpretation errors
### Error 7: reading one [[peso_boltzmann]] as an occupation
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

### Error 8: interpreting [[energia_media]] without probabilities
**Why it seems correct**
It seems plausible because [[funcion_particion]] is compact and appears close to [[probabilidad_microestado]] in the calculation chain.

**Why it is incorrect**
The [[funcion_particion]] is not a direct occupation. It sums [[peso_boltzmann]] values and works as a normalizer. Its physical meaning depends on [[temperatura]], [[beta]], and [[energia_microestado]].

**Detection signal**
The result is interpreted without checking positivity, normalization, or the role of thermal scale.

**Conceptual correction**
Compute weights first, then sum [[funcion_particion]], normalize each [[probabilidad_microestado]], and only then interpret [[energia_media]].

**Mini-example of contrast**
Two microstates may exist, but if one has large [[energia_microestado]] at low [[temperatura]], its [[peso_boltzmann]] can be nearly zero even though it is listed as accessible.

## Quick self-control rule
Before accepting a canonical calculation, check that all [[peso_boltzmann]] values are positive, the total [[probabilidad_microestado]] is one, and [[energia_media]] lies inside the accessible energy range.

